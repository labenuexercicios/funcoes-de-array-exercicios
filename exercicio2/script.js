const array = [1, 2, 3, 4, 5, 6, 7, 8]

const triplos = array.map((numbers) => {
    return numbers * 3
})

const metades = array.map((numbers) => {
    return numbers / 2
})

console.log(triplos)
console.log(metades)